[workspace]
members = [
    "session_gate",
    "dispute",
    "skills_mirror",
    "stake",
    "release_auth",
    "reputation",
    "common_events",
]
exclude = [
    "fee_split",
    "withdrawal",
]
resolver = "2"

[profile.release]
panic = "abort"
lto = true
